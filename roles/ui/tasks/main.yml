---

- include: sway.yml
  tags: ['ui','sway']

- include: fonts.yml
  tags: ['fonts']

- include: desktop.yml
  tags: ['desktop']

- include: search.yml
  tags: ['search']

- name: install themes
  aur:
    use: pacaur
    name: "{{ theme_pkgs }}"
    state: present
  become: True
  become_user: "{{ unix_admin.name }}"
  tags: ['theme_pkgs']

- name: install ui_pkgs
  aur:
    use: pacaur
    name: "{{ ui_pkgs }}"
    state: present
  become: True
  become_user: "{{ unix_admin.name }}"
  tags: ['ui_pkgs']
