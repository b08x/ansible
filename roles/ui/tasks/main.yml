---

- include: sway.yml
  tags: ['ui','sway']

- include: fonts.yml
  tags: ['fonts']


# - include: rofi.yml
#   # when: not rofi_bin.stat.exists
#   tags: ['rofi']

#- name: check if compton is already installed, if not then dont.
#  stat: path="/usr/bin/compton"
#  register: compton_bin

#- include: compton.yml
#  when: not compton_bin.stat.exists
#  tags: ['compton']

- name: install audio tools from aur
  aur: name= {{ item }} user={{ unix_admin.name }}
  with_items:
    - oomox
    - keyboardlayouteditor-git
  tags: ['utilpkgs']
