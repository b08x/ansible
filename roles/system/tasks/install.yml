---

# packages to install on any system
- name: install packages
  package:
    name: "{{ item }}"
    state: present
    use: '{{ package_manager }}'
  with_items:
    - timeshift
    - firefox
    - bat
    - terminator
    - htop
    - barrier
    - rofi
    - samba
    - bind
    - zim
    - lsd
    - ranger
    - vim
    - bash-completion
    - privoxy

# - aur:
#     use: yay
#     name:
#       -
#   become: yes
#   become_user: aur_builder
#   tags: ['aur']
